const friends = ['rohim', 'korim', 'josim', 'akkas'];
const [f1, f2, f3, f4] = friends;
// console.log(f2);

const student = {
    name: 'akas',
    roll: 100021,
    sFriends: ['korim', 'josim'],
    age: 18,
}

const { age, name, sFriends, roll } = student;
console.log(sFriends);

